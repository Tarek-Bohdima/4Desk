name: Flutter Nightly Build

# 4desk: no nightly builds on this fork; run manually if needed.
on:
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: "nightly"
